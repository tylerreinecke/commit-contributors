Testing by changing the GIT_COMMITTER_NAME env var
 - export GIT_COMMITTER_NAME="Spoof Name"
 - git commit -m "only spoofed name env var"
 - FAIL --> I still show up as committer and green verified pill still present


Testing by changing the GIT_COMMITTER_EMAIL env var
 - export GIT_COMMITTER_EMAIL="spoofemail@asdfasdfasdfasdf.com"
 - git commit -m "only spoofed email env var"
 - SUCCESS --> my account shown as author, and just my name (with grey pfp) set at committer
 - NOTE: "Unverified" Orange pill


Testing by changing both env vars
 - export GIT_COMMITTER_NAME="Sir Spoof III"
 - export GIT_COMMITTER_EMAIL="spoofemail@asdfasdfasdfasdf.com"
 - git commit -m "spoofed both env vars"
 - SUCCESS --> my account shown as author, and fake name (with grey pfp) set at committer
 - NOTE: "Unverified" Orange pill

Testing setting Max as Committer via env vars
 - export GIT_COMMITTER_NAME="Max Fusco"
 - export GIT_COMMITTER_EMAIL="max.fusco@datadoghq.com"
 - git commit -m "impersonate max via both env vars"
 - SUCCESS --> my account shown as author, and Max's account shown as committer
 - NOTE: "Unverified" Orange Pill